## v0.1.0

- adds `lti today` command to get the entry for today's date (or a specified date) in the liturgical calendar
- adds `lti build` command to build an ICalendar or Markdown file for a given year and plan
- adds `lti validate` command to validate a plan file against the calendar
